import React from "react";
import css from "./{{{NAME}}}.module.scss";

type Props = {};

const {{{NAME}}} = (props) => {
  return <Presenter />;
};

type PresenterProps = {};

export const Presenter = (props: PresenterProps) => {
  return <div style={`${css["comp"]}`}></div>;
};

export default {{{NAME}}};
